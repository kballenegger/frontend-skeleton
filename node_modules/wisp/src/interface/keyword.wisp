(ns wisp.interface.keyword
  (:refer [wisp.interface.protocol :refer [satisfies?]]))

(defprotocol IKeyword
  (-keyword [source] "identifier unique to the keyword"))


(defn keyword?
  [x]
  (satisfies? IKeyword x))
