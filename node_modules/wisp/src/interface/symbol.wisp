(ns wisp.interface.symbol
  (:refer [wisp.interface.protocol :refer [satisfies?]]))

(defprotocol ISymbol
  "Interface for symbol data types"
  (-id [source]))

(defn symbol?
  [x]
  (satisfies? ISymbol x))
