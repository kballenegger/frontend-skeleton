(ns wisp.interface.vector
  (:require [wisp.interface.protocol :refer [satisfies?]]))

(defprotocol IVector
  "Interface for vector data types"
  (^clj -assoc-n [source n value]))

(defn vector?
  [x]
  (satisfies? IVector x))
