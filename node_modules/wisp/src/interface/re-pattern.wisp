(ns wisp.interface.re-pattern
  (:refer [wisp.interface.protocol :refer [satisfies?]]))

(defprotocol IRePattern
  (-source [pattern])
  (re-find [pattern text])
  (re-matches [pattern text])
  (re-seq [pattern text]))

(defn re-pattern?
  [x]
  (satisfies? IRePattern x))
