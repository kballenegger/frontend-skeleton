(ns wisp.core.string
  (:require [wisp.type.string :refer [IString StringWithMetadata
                                      subs split split-lines blank?
                                      join upper-case lower-case capitalize
                                      replace-first replace triml trimr trim
                                      string?]]))

(defn str
  [& chunks]
  (join "" chunks))
